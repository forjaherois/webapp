import React from 'react';
import { NextUIProvider } from '@nextui-org/react';

const App = () => {
    return (
        <NextUIProvider>
            <h1>Hello World</h1>
        </NextUIProvider>
    );
};

export default App;
